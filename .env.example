# ==========================================
# AI-SaaS 环境变量配置
# ==========================================

# ==========================================
# 数据库配置 (Supabase)
# ==========================================
# 从 Supabase Dashboard > Settings > Database > Connection string 获取
# 格式: postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxx.supabase.co:5432/postgres
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.xxxxxx.supabase.co:5432/postgres"

# Supabase API配置（可选，如果使用Supabase客户端）
NEXT_PUBLIC_SUPABASE_URL="https://xxxxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# ==========================================
# NextAuth 认证配置
# ==========================================
# 生成方法: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# ==========================================
# OAuth 提供商 (Google)
# ==========================================
# 从 Google Cloud Console 获取
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ==========================================
# 支付配置 (PayPal)
# ==========================================
# 开发环境 (沙箱):
#   1. 访问 https://developer.paypal.com/dashboard/
#   2. 创建沙箱应用
#   3. 获取Client ID和Secret (以sb-开头)
# 
# 生产环境:
#   1. 访问 https://www.paypal.com/businessmanage/account/aboutBusiness
#   2. 设置 → API访问权限 → 创建应用
#   3. 获取Live Client ID和Secret (以AX/EL开头)
#   4. 将PAYPAL_MODE改为"live"
# 
# 详细说明: 查看 docs/PAYPAL_PRODUCTION_SETUP.md

PAYPAL_MODE="sandbox"                          # 开发: sandbox | 生产: live
PAYPAL_CLIENT_ID="your-paypal-client-id"       # 沙箱: sb-xxx | 生产: AXxxx
PAYPAL_CLIENT_SECRET="your-paypal-secret"      # 沙箱: xxx | 生产: ELxxx

# PayPal公开密钥（用于前端SDK）
NEXT_PUBLIC_PAYPAL_CLIENT_ID="your-paypal-client-id"  # 与上面的CLIENT_ID相同

# PayPal Webhook (可选，用于接收支付通知)
PAYPAL_WEBHOOK_ID="your-webhook-id"

# ==========================================
# 支付配置 (Stripe - 订阅支付)
# ==========================================
# 从 Stripe Dashboard 获取
# https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLIC_KEY="pk_test_xxx"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_xxx"
STRIPE_SECRET_KEY="sk_test_xxx"
STRIPE_WEBHOOK_SECRET="whsec_xxx"
STRIPE_PRICE_PLUS_MONTHLY="price_plus_monthly"
STRIPE_PRICE_PLUS_YEARLY="price_plus_yearly"
STRIPE_PRICE_ULTRA_MONTHLY="price_ultra_monthly"
STRIPE_PRICE_ULTRA_YEARLY="price_ultra_yearly"

# ==========================================
# 外部API配置
# ==========================================
# Serper.dev API (Reddit搜索)
SERPER_API_KEY="66c8fcd3f7280a42e045cce7193382a6fd64125a"
SERPER_API_URL="https://google.serper.dev/search"
SERPER_TIMEOUT="10000"

# 腾讯ADP
TENCENT_ADP_APP_KEY="your-adp-app-key"
TENCENT_ADP_API_URL="https://adp.tencent.com/api/chat"

# ==========================================
# 邮件服务配置 (Contact Form)
# ==========================================
# 方案1: 使用 Brevo (Sendinblue) - 推荐
# 1. 注册 https://www.brevo.com/
# 2. 获取 API Key: Account > SMTP & API > API Keys
# 3. 每天 300 封邮件免费
BREVO_API_KEY="your-brevo-api-key"
BREVO_FROM_EMAIL="noreply@yourdomain.com"

# 方案2: 使用 Resend - 备用
# 1. 注册 https://resend.com/
# 2. 获取 API Key
# 3. 每天 100 封邮件免费
RESEND_API_KEY="your-resend-api-key"
RESEND_FROM_EMAIL="noreply@yourdomain.com"

# ==========================================
# LLM 服务配置 (关键词提取)
# ==========================================
# 用于从用户输入中智能提取关键词
# 
# 方案1: Google Gemini (推荐)
# 1. 访问 https://makersuite.google.com/app/apikey
# 2. 创建 API Key
# 3. 免费额度: 每分钟 60 次请求
GEMINI_API_KEY="your-gemini-api-key"
GEMINI_MODEL="gemini-1.5-flash"

# 方案2: OpenAI (备用)
# OPENAI_API_KEY="your-openai-api-key"
# OPENAI_MODEL="gpt-3.5-turbo"

# AI 试衣图像生成
OPENAI_API_KEY="your-openai-api-key"
OPENAI_IMAGE_MODEL="gpt-image-1"
OPENAI_IMAGE_SIZE="1024x1024"

# AI 走秀视频生成（单独维护，不与图像生成配置混用）
WALK_VIDEO_API_KEY="your-walk-video-api-key"
WALK_VIDEO_BASE_URL="https://new.12ai.org"
WALK_VIDEO_MODEL="grok-imagine-video-1.5-preview"

# 图片、视频资产存储
# 本地开发可使用 local；Railway 线上建议使用 r2
STORAGE_PROVIDER="local" # local | r2

# 本地存储配置，仅 STORAGE_PROVIDER="local" 时使用
LOCAL_UPLOAD_DIR="./uploads"
LOCAL_UPLOAD_PUBLIC_BASE_URL="/api/assets"

# Cloudflare R2 配置，仅 STORAGE_PROVIDER="r2" 时使用
R2_ACCOUNT_ID="your-cloudflare-account-id"
R2_ACCESS_KEY_ID="your-r2-access-key-id"
R2_SECRET_ACCESS_KEY="your-r2-secret-access-key"
R2_BUCKET_NAME="vfitly-assets"
R2_PUBLIC_BASE_URL="https://assets.vfitly.com"
# 可选：默认会自动使用 https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com
# R2_ENDPOINT="https://your-cloudflare-account-id.r2.cloudflarestorage.com"

# ==========================================
# Redis 配置 (可选，用于缓存和限流)
# ==========================================
# 可以使用 Upstash Redis 免费层
# https://upstash.com/
REDIS_URL="redis://localhost:6379"
UPSTASH_REDIS_REST_URL="https://xxx.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-token"

# ==========================================
# 应用配置
# ==========================================
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
